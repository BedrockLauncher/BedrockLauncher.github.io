/*function LOCALWindowOnload () {
    var hiddenAns = document.getElementsByClassName("hidden-ans");
    var hiddenAnsIndex = 0;
    while (hiddenAnsIndex < hiddenAns.length) {
        hiddenAns[hiddenAnsIndex].setAttribute("id", )
        
        hiddenAnsIndex++;
    }
}

if (window.onloadArray) {
    window.onloadArray.push(LOCALWindowOnload);
} else {
    window.onloadArray = [LOCALWindowOnload];
}
*/